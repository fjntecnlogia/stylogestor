import { OnboardingFlow } from '@/components/onboarding/onboarding-flow'

export const metadata = { title: 'Configurar sua barbearia — STYLOGESTOR' }

export default function OnboardingPage() {
  return <OnboardingFlow />
}
