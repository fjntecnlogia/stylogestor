legacy-peer-deps=true
ignore-scripts=true

# Workaround Windows MAX_PATH + Metro release pnpm path resolution:
# Layout flat (yarn-style) elimina o .pnpm/<long-hash>/ que quebra
# build release nativo + Metro createBundleReleaseJsAndAssets.
node-linker=hoisted
