# deps
node_modules
.pnpm-store

# build
.next
dist
build
.turbo

# env
.env
.env.local
.env.production

# Credenciais — NUNCA commitar
google-services-key.json
**/google-services-key.json
*-service-account*.json

# Upload keystores (Play Store / iOS App Store) - NUNCA commitar
# Mantemos debug.keystore (gerado pelo Expo, comum a todos os devs)
upload-keystore.jks
release.keystore
*-upload.jks
**/keystore.properties

# prisma
prisma/dev.db
prisma/dev.db-journal

# misc
.DS_Store
*.log
*.logcd
.vscode

# Builds Android/iOS — binários pesados, NUNCA commitar (gerar via EAS)
*.aab
*.apk
*.ipa
build-*.aab
build-*.apk

# Backups automáticos de assets/config (gerados por Expo/Vercel)
*.expo-default.bak
*.vercel-default.bak
*.bak

# Arquivos temporários
_tmp_*
**/_tmp_*

# Claude Code — config local por dev (CLAUDE.md por pasta CONTINUA versionado)
.claude/
.expo/
